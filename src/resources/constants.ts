export const toastOptions = {
  style: {
    minWidth: 300,
    fontFamily: "Mardoto-Regular",
  },
  duration: 3000,
};

export const defaultFilterValues = {
  pageNumber: 1,
  sortColumn: "",
  sortDirection: "asc",
  pageSize: 10,
};

export enum EModalTypes {
  OPEN = "open",
  UPDATE = "update",
}

export const RIGHT_SIDEBAR_WIDTH = 240;
